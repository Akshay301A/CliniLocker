# Supabase Configuration (safe in frontend – anon key is public by design)
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Do NOT put OpenAI API key here or in any VITE_ variable – it would be exposed in the browser.
# Use it only in backend / Supabase Edge Function secrets (e.g. OPENAI_API_KEY).
